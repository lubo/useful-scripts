#!/bin/bash
#
# Converts bookmarks to a more diff-friendly format with irrelevant fields
# removed.

set -eo pipefail

bookmarks2json < "${1:?}" \
	| yq \
	--width 2147483647 `# -1 doesn't work` \
	--yaml-output \
	'map(del(.last_modified, .metadata."Last check"))'
